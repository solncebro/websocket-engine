{
  "name": "@solncebro/websocket-engine",
  "version": "0.4.0",
  "description": "Reliable WebSocket client with reconnect, heartbeat and typed messages",
  "license": "ISC",
  "repository": { "type": "git", "url": "https://github.com/solncebro/websocket-engine" },
  "keywords": ["websocket", "reconnect", "heartbeat", "browser", "react-native", "typescript"],
  "engines": { "node": ">=22" },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "prepublishOnly": "yarn build"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.4.5"
  }
}
